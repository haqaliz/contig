# Plan: doi-fetch (2026-08-27)

Aspect of `reproduce-doi-pdf-intake`. Source: `prd.md` M2, R3 + `doi-fetch/spec.md`.
Owner: aliz. Branch: `feat/reproduce-doi-pdf-intake/aliz`.

## Setup checklist

- Worktree `.claude/worktrees/feat-reproduce-doi-pdf-intake`, `uv sync` done.
- No new dependency (stdlib `urllib.request`, `html.parser`); `uv.lock`
  unchanged.
- New/changed files: `src/contig/verification/paper_intake.py` (append),
  `tests/test_paper_intake.py` (append).
- Commits build on the `intake-classifier` aspect commit (same branch).

## Phase 1: RED — pure `resolve_pdf_url` + `_doi_url` + `_looks_like_pdf` tests

- **Goal:** failing tests for the three pure functions (no network anywhere).
- **Prerequisites:** `intake-classifier` merged into the branch (the module
  file exists).
- **Files:** `tests/test_paper_intake.py` (new section; keep classifier tests
  untouched).
- **Steps:**
  1. `test_doi_url_builder`: `_doi_url("10.1038/x")` ==
     `"https://doi.org/10.1038/x"`; verbatim (no stripping/quoting).
  2. Fixture HTML strings for `resolve_pdf_url`:
     - single meta, `name` before `content`; single meta, `content` before
       `name`; `NAME="CITATION_PDF_URL"` uppercase; `name='citation_pdf_url'`
       single quotes — each returns the URL.
     - no meta at all → `PaperIntakeError`; two distinct non-empty metas →
       `PaperIntakeError` with `"2"` in the message; one empty-content meta →
       `PaperIntakeError` (absent); unrelated metas (`citation_title`,
       `citation_author`) ignored.
  3. `_looks_like_pdf`: `b"%PDF-1.4\n..."` → True; `b"<!DOCTYPE html>"` →
       False; `b""` → False.
- **Validation:** `uv run pytest tests/test_paper_intake.py` — RED.

## Phase 2: GREEN — implement the pure functions

- **Files:** `src/contig/verification/paper_intake.py`.
- **Steps:**
  1. `class PaperIntakeError(ValueError)`.
  2. `_doi_url(doi) -> str`.
  3. `_PdfUrlParser(HTMLParser)` subclass: `handle_starttag` collecting
     `name`/`content` attrs case-insensitively; non-empty contents only.
  4. `resolve_pdf_url(html) -> str`: parser run; 0 → raise absent-message;
     >1 → raise naming the count; 1 → return it.
  5. `_looks_like_pdf(data: bytes) -> bool` (magic prefix).
- **Validation:** `uv run pytest tests/test_paper_intake.py` green; full suite
  green.

## Phase 3: RED → GREEN — `default_paper_fetcher` seam + shape pins

- **Goal:** the seam type exists, the default is honest on every give-up, and
  the urllib path is shape-asserted but **never executed in CI**.
- **Files:** `src/contig/verification/paper_intake.py`,
  `tests/test_paper_intake.py`.
- **Steps:**
  1. RED: `test_default_paper_fetcher_is_callable_and_never_runs_in_ci` —
     assert `default_paper_fetcher` is a callable of arity 2; assert its
     internal urllib entry point is monkeypatchable (module-level
     `_urlopen` name exists — the test patches `paper_intake._urlopen` and
     asserts a `RuntimeError` propagates as `(nonzero, message)` so a stray
     real call can never slip through silently).
  2. RED: `test_default_paper_fetcher_success_pdf_first` — patch
     `paper_intake._urlopen` with a fake whose response has
     `headers["Content-Type"] = "application/pdf"` and body `%PDF…`; assert
     `(0, …)` and `dest` bytes == the PDF body.
  3. RED: `test_default_paper_fetcher_landing_page_path` — fake response 1:
     HTML with one `citation_pdf_url`; fake response 2: the PDF; assert
     `(0, …)` and `dest` == PDF bytes; the PDF URL is fetched.
  4. RED: give-up pins — landing page without the meta → non-zero message
     containing "paywalled" and "path"; duplicate metas → non-zero message
     naming the count; `urlopen` raising (`URLError`) → non-zero with the
     reason; download body over the landing-page read bound → non-zero naming
     the bound; a partial `dest` is removed on failure (assert `not
     dest.exists()`).
  5. GREEN: implement `PaperFetcher` type alias, `_urlopen` module-level
     wrapper, `default_paper_fetcher(doi, dest)` per the spec (PDF-first
     sniff → meta path → bounded reads, `dest` cleanup on every give-up,
     `timeout=30`).
- **Validation:** `uv run pytest tests/test_paper_intake.py` green; full suite
  green.

## File-by-file build order

1. `tests/test_paper_intake.py` — append pure-function tests (Phase 1), then
   seam tests (Phase 3).
2. `src/contig/verification/paper_intake.py` — `PaperIntakeError`,
   `_doi_url`, `_PdfUrlParser`, `resolve_pdf_url`, `_looks_like_pdf`,
   `PaperFetcher`, `_urlopen`, `default_paper_fetcher`.

## Testing strategy

- Unit: pure functions (Phase 1) — deterministic fixture strings/bytes.
- Seam: `_urlopen` monkeypatched with fake responses (Phase 3) — no real
  network; the default's failure semantics pinned.
- Integration: none in this aspect (the CLI-level scripted-fetcher tests land
  in `cli-wiring`).

## Environment & reproducibility

- Env: none. External tools: none (urllib, not git).
- Determinism: fixture bytes only; no sockets opened; `timeout=30` constant
  with its own pin (assert the fake `_urlopen` was called with a `timeout`
  kwarg).

## Edge cases & error handling

Per the spec: PDF-first sniff, absent/duplicate metas, HTTP/DNS errors,
oversized landing page, partial-file cleanup. The paywall message wording is
product copy — pinned verbatim by a test substring.

## Agent execution notes

- This aspect can run **in parallel** with `pdf-text-seam` (independent
  files/modules); coordinate so both merge before `cli-wiring` starts.
- Do not touch `src/contig/fetch.py`, `runner.py`, `models.py`, or the
  reproduce test files.
- Checkpoint: full suite green after each phase; the seam's non-zero message
  strings are load-bearing for `cli-wiring` — keep them stable (they are the
  refusal text users see).

## Commit message

`feat(intake): stdlib DOI-to-PDF fetch with citation_pdf_url resolution (doi-fetch)`