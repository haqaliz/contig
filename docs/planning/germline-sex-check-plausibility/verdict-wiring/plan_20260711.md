# Implementation Plan — verdict-wiring (2026-07-11)

Aspect: `docs/planning/germline-sex-check-plausibility/verdict-wiring/spec.md`
Depends on: **inference-core** (complete) — imports `evaluate_sex_plausibility`.
Executed in Phase 6 under strict TDD.

Small, surgical: extend the existing germline `_discover_qc` gate so `sex_plausibility`
reaches the verdict on every germline run, and prove it cannot change the exit code.

## Confirmed facts from the dig (do not re-derive)

- `_discover_qc` lives in `src/contig/runner.py`; the germline block is at **runner.py:254-264**,
  `if assay == "variant_calling":` — it locates the VCF via
  `manifest_for("variant_calling").required[0]` rglob'd under `run_dir` and does
  `results.extend(evaluate_variant_plausibility(vcfs[0]))`. **Reuse that same `vcfs[0]`.**
- Import site: beside `runner.py:60`
  (`from contig.verification.variant_metrics import evaluate_variant_plausibility`).
- Public API to call: `from contig.verification.sex_plausibility import evaluate_sex_plausibility`.
- Verdict/exit contract (already confirmed): `overall_verdict` precedence
  `fail>warn>pass>unverified` (`models.py:78-96`); `contig run` exit is decided ONLY by
  pipeline success (`cli.py:610-612`), never by the QC verdict. A WARN/UNVERIFIED
  `sex_plausibility` therefore cannot change the exit code.
- `contig verify` does NOT re-run the QC packs — the check flows through the `run` path only.
  Do not touch `verify`.

## Files to modify

| File | Action |
|---|---|
| `src/contig/runner.py` | add import; add one `results.extend(evaluate_sex_plausibility(vcfs[0]))` line in the germline block |
| `tests/…` (the existing `_discover_qc` germline test) | extend/add an integration test |

## Implementation phases (RED → GREEN → REFACTOR)

### Phase 1 — integration test (RED) then wire (GREEN)

- **Goal:** `sex_plausibility` + informational `x_het_ratio` appear in `_discover_qc` output for
  a germline run; absent for non-germline; existing `ts_tv`/`het_hom` unregressed; exit-code
  invariant holds.
- **Prerequisites:** find the current test that exercises the `variant_calling` `_discover_qc`
  germline gate (search `tests/` for `_discover_qc` + `variant_calling` / `evaluate_variant_plausibility`;
  likely `tests/test_runner*.py` or `tests/verification/`). Extend it rather than duplicating
  the run-dir/manifest harness.
- **RED tests:**
  1. Place a **discordant** germline VCF fixture (autosomal-level chrX het + several chrY
     variants) at the manifest's required glob under a temp run dir; call
     `_discover_qc(run_dir, "variant_calling")`; assert the results contain a
     `sex_plausibility:*` with status `warn` AND an `x_het_ratio:*` informational result,
     **alongside** the existing `ts_tv`/`het_hom` results (assert those still present — no
     regression). Reuse the inference-core test's VCF-builder helpers or a shared fixture.
  2. Call `_discover_qc(run_dir, "rnaseq")` (or another non-germline assay) with no germline
     VCF → assert **no** `sex_plausibility:*` result (gate holds).
  3. Verdict-invariance: with a results list containing only a WARN `sex_plausibility`,
     `overall_verdict(results)` is `"warn"` (not `"fail"`); reference/confirm the existing
     exit-path test that a WARN verdict does not raise `typer.Exit` (cover via the established
     pattern — do not add a new subprocess run).
  - Confirm these FAIL before wiring (RED: no `sex_plausibility` emitted yet).
- **GREEN:** add the import beside runner.py:60 and, in the germline block (runner.py:254-264),
  after the `evaluate_variant_plausibility(vcfs[0])` extend, add
  `results.extend(evaluate_sex_plausibility(vcfs[0]))`. No second discovery.
- **REFACTOR:** none expected (one line + import).
- **Validation:** `uv run pytest <the touched test file>` then `uv run pytest` (full suite).
- **Commit:** `test+feat(verify): wire sex_plausibility into germline _discover_qc [verdict-wiring]`

## Testing strategy

- One integration test through `_discover_qc`. No new engine subprocess. Assert presence +
  gate + non-regression + verdict-invariance.
- Commands: `uv run pytest` (full suite must stay green — currently 1438).

## Edge cases

- No germline VCF located → the block already falls through silently (structural QC owns the
  missing-required-output case); do NOT add a false UNVERIFIED here.
- inference-core already handles the VCF-content edge cases; this aspect only routes.

## Agent execution notes

- The only real work is locating the correct existing `_discover_qc` germline test to extend.
  If none exists in a form that's cheap to extend, add a focused new one using the same
  run-dir/manifest harness the somatic/rnaseq `_discover_qc` tests use.
- Trivial diff → cheapest capable model; the judgment is entirely "find and extend the right
  test."
